<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Toggled ON (pinned) -->
    <item android:state_selected="true" android:drawable="@drawable/button_pinned"/>
    <!-- Untoggled (default) -->
    <item android:drawable="@drawable/button_pin"/>
</selector>
