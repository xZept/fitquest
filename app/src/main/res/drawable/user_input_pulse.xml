<?xml version="1.0" encoding="utf-8"?>
<animation-list xmlns:android="http://schemas.android.com/apk/res/android"
    android:oneshot="true">

    <!-- brief default -->
    <item android:drawable="@drawable/bg_user_input" android:duration="80"/>

    <!-- flash selected -->
    <item android:drawable="@drawable/bg_user_input_selected" android:duration="120"/>

    <!-- back to default (we may override to selected in code if it has value) -->
<!--    <item android:drawable="@drawable/bg_user_input" android:duration="160"/>-->
</animation-list>
