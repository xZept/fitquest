<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dialog_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Animated background spans the whole dialog -->
    <ImageView
        android:id="@+id/iv_rest_bg"
        android:layout_width="match_parent"
        android:layout_height="550dp"
        android:adjustViewBounds="false"
        android:scaleType="centerCrop" />

    <!-- Foreground content on top of the animated bg -->
    <LinearLayout
        android:id="@+id/dialog_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/tv_rest_countdown"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0:30"
            android:textColor="@android:color/white"
            android:layout_gravity="center"
            android:textSize="70sp"
            android:layout_marginBottom="25dp"
            android:paddingTop="4dp" />

        <ImageButton
            android:id="@+id/btn_skip_rest"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:layout_marginTop="12dp"
            android:src="@drawable/button_skip"
            android:scaleType="fitCenter"
            android:layout_gravity="center"
            android:adjustViewBounds="true"
            android:background="@drawable/button_skip" />

    </LinearLayout>
</FrameLayout>
